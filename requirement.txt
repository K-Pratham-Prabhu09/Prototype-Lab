# --- Core Deep Learning Frameworks ---
torch
torchvision
torchaudio
tensorflow
keras

# --- Deep Reinforcement Learning ---
# NOTE: On Windows, `gymnasium[all]` pulls in `uvloop` (which cannot be built).
# Use a smaller set of extras to avoid uvloop.
gymnasium[atari,box2d]       # The standard API for RL environments (without uvloop)
stable-baselines3[extra] # Reliable implementations of RL algorithms (PPO, DQN, etc.)
shimmy               # Compatibility layer for older Gym environments
pettingzoo           # For Multi-Agent Reinforcement Learning (MARL)

# --- Data Science & Visualization ---
numpy
pandas
matplotlib
seaborn
scikit-learn
tqdm                 # Progress bars for training loops

# --- Experiment Tracking ---
tensorboard
wandb                # Weights & Biases for cloud-based logging
mlflow